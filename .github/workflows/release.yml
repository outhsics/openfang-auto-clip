name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run tests
        run: python -m unittest discover -s tests

      - name: Prepare release bundle
        run: python scripts/release_prep.py "${GITHUB_REF_NAME}" --skip-tests --allow-dirty

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "OpenFang Auto Clip ${GITHUB_REF_NAME}" \
            --notes-file "dist/releases/${GITHUB_REF_NAME}/release_notes.md" \
            "dist/releases/${GITHUB_REF_NAME}/benchmark_report.json" \
            "dist/releases/${GITHUB_REF_NAME}/preview.png" \
            "dist/releases/${GITHUB_REF_NAME}/storyboard.png" \
            "dist/releases/${GITHUB_REF_NAME}/launch_post.md" \
            "dist/releases/${GITHUB_REF_NAME}/github_social_preview.svg" \
            "dist/releases/${GITHUB_REF_NAME}/github_social_preview_zh.svg" \
            "dist/releases/${GITHUB_REF_NAME}/bundle_manifest.json"
