# Launch Assets

This folder stores persistent launch copy that should remain usable after a single release run finishes.

## Current Pack

- [v0.3.0/README.md](v0.3.0/README.md)

Use `dist/releases/` for generated release artifacts.
Use `docs/launch/` for durable copy and posting guidance.
